/**
 * Master Data (MAS) — module-specific UI components (skill §2.1). Empty in the scaffold;
 * per-screen briefs add the screen's components here. No cross-feature imports
 * (lint-enforced import boundary, skill §2.4).
 */
export {};
