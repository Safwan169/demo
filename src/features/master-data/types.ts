/**
 * View-model types for the Master Data (MAS) feature (skill §2.1). These are the UI-facing
 * types (NOT the generated wire types in lib/api/generated). Empty in the
 * scaffold — each per-screen brief adds the types its screen needs.
 */
export {};
