/**
 * Re-export shim — the designed LoginCard lives in features/audit/components/.
 * Kept for any existing imports (tests, etc.) that reference this path.
 */
export { LoginCard as LoginForm } from "@/features/audit/components/LoginCard";
